[200~# Question 2 - Secure Project Workspace Setup

## Command 1

### Command

```bash
mkdir ProjectWorkspace
```

### Output

```
(No output because the directory was created successfully.)
```

### Explanation

The `mkdir` command creates a new directory named `ProjectWorkspace`, which serves as the main workspace for storing project files.

---

## Command 2

### Command

```bash
cd ProjectWorkspace
```

### Output

```
(No output)
```

### Explanation

The `cd` command changes the current working directory to `ProjectWorkspace`.

---

## Command 3

### Command

```bash
mkdir Documents Source Backup
```

### Output

```
(No output)
```

### Explanation

This command creates three directories to organize project documentation, source code, and backup files.

---

## Command 4

### Command

```bash
touch Documents/plan.txt
```

### Output

```
(No output)
```

### Explanation

The `touch` command creates an empty file named `plan.txt` inside the Documents directory.

---

## Command 5

### Command

```bash
ls -ld Documents Source Backup
```

### Output

```
drwxr-xr-x ...
drwxr-xr-x ...
drwxr-xr-x ...
```

### Explanation

This command displays detailed information about the directories, including permissions, ownership, and timestamps.

---
